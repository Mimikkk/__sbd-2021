import { Grid } from '@mui/material';

export const Tile = () => {
  return (
    <Grid
      sx={{
        justifyContent: 'center',
        display: 'flex',
        justifyItems: 'center',
        alignItems: 'center',
        height: '100vh',
        width: '100vw',
      }}
    >
    </Grid>
  );
};